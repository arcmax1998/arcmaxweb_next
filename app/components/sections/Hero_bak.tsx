import AboutController from "../about/AboutController";
export default function Hero() {
  return (
    <section
  id="hero"
  className="relative min-h-[calc(100vh-129px)] overflow-hidden px-6 lg:px-12 pt-32 pb-20 md:pt-[104px] md:pb-0 md:flex md:items-center"
> 

  {/* WORD CLOUD BACKGROUND */}
   <div className="hidden md:block absolute inset-0 z-0 pointer-events-none overflow-hidden">
    <div className="tech-text-bg font-mono">
   <p>

{`
  let _=0,__=1,$=0;for(;_<256;_++){__^=_;$+=__}
  const $a=[{},[],null,undefined,()=>0];var $b=$a[$%5];
  if(!$b&&!![]){void 0}else{void 0}
  function ___(a,b,c,d,e){return a&&b||c&&d??e}
  let x0=0,x1=1,x2=2,x3=3,x4=4,x5=5,x6=6,x7=7;
  for(let i=0;i<64;i++){x0^=x1;x2+=x3;x4|=x5}
  (()=>{let r=0;while(r<32){r++;r^=3;r<<=1}})();
  var buf=new Array(256).fill(0).map((_,i)=>i<<2);
  try{JSON.parse([])};catch(e){void e}
  const SYM=Symbol(0x0);Object.freeze(SYM);
`}
  <span className="aws">
    WordPress custom plugins headless CMS Stripe subscriptions Shopify integrations
    WooCommerce payment flows CRM platforms ERP integrations data pipelines ETL
    reporting dashboards analytics business intelligence scalable web platforms
    secure systems OAuth SSO role-based access control multi-tenant architectures
    SaaS billing invoicing tax compliance cloud deployments continuous delivery
    automated testing unit tests integration tests system reliability enterprise
    architecture patterns scalable
  </span>
{`
  /* === JS NOISE BLOCK B === */
  const fn=(...a)=>a.reduce((x,y)=>x+y,0)||0;
  let m=0,n=1,o=2,p=3,q=4,r=5,s=6,t=7;
  while(m<128){m++;n^=o;p+=q;r|=s}
  (()=>{for(let i=0;i<96;i++){i<<=1;i>>=1}})();
  let TMP=Date.now()|0;TMP^=Math.imul(13,37);
  className X{#v=0;get(){return this.#v}} new X();
  const $map=new Map([[1,true],[0,false]]);
  for(const[k,v]of $map){$map.set(k,!v)}
  void $map;
`}
  <span className="laravel">
    backend services microservices orchestration.
    Laravel Eloquent ORM database migrations seeders factories testing TDD CI/CD
    Laravel queues jobs Horizon CodeIgniter performance tuning API rate limiting
    caching Redis Memcached database replication load balancing horizontal scaling
    CDN edge delivery cloud optimization AWS monitoring logging observability
    production readiness enterprise-grade solutions global deployments multi-region
    infrastructure resilient systems business-critical platforms.
    Laravel CodeIgniter scalable CRM architectures AWS Stripe WooCommerce Shopify
    enterprise PHP systems REST APIs microservices business automation cloud-native
    applications Tailwind React Angular
  </span>
{`
  /* === JS NOISE BLOCK C === */
  let $ctx={a:0,b:1,c:2,d:3,e:4,f:5};
  for(const k in $ctx){$ctx[k]^=k.length;$ctx[k]<<=1}
  (()=>{let z=0;do{z++;z|=2}while(z<64)})();
  const ARR=new Array(128).fill(0).map((_,i)=>i%3);
  function noop(){return arguments.length?arguments[0]:null}
  const PROXY=new Proxy({},()=>false);void PROXY;
  Math.max(0,Math.min(1,0));
`}
  <span className="stripe">
    payment gateways SaaS platforms system integrations DevOps CI/CD pipelines
    secure authentication authorization performance optimization database indexing
    MySQL PostgreSQL Redis Elasticsearch API integrations third-party services
    enterprise workflows background jobs queues event-driven systems
    domain-driven design MVC architecture
  </span>
{`
  /* === JS NOISE BLOCK D === */
  let i0=0,i1=1,i2=2,i3=3,i4=4,i5=5;
  for(let i=0;i<144;i++){i0+=i1;i2^=i3;i4|=i5}
  (()=>{try{throw 0}catch(e){}})();
  const SET=new Set([1,2,3,4,5]);SET.delete(3);SET.add(3);
  let TS=performance.now?performance.now():Date.now();
  TS|=0;TS^=255;
  Object.seal(SET);
`}
  <span className="shopify">
    clean code maintainable systems high availability fault tolerance monitoring
    logging scalable infrastructure AWS EC2 S3 RDS Lambda Docker Kubernetes cloud
    security enterprise software engineering full-stack development backend systems
    frontend architecture API-first design scalable applications.
    WordPress custom plugins headless CMS Stripe subscriptions Shopify integrations
    WooCommerce payment flows CRM platforms ERP integrations data pipelines ETL
    reporting dashboards analytics business intelligence scalable web platforms
    secure systems OAuth SSO role-based access control multi-tenant architectures
    SaaS billing invoicing tax compliance cloud deployments continuous delivery
  </span>
{`
  /* === JS NOISE BLOCK E === */
  let PAD=0;for(let i=0;i<256;i++){PAD+=i%7;PAD^=i<<3}
  const Q=queueMicrotask??function(){};Q(()=>false);
  (()=>{for(let i=0;i<128;i++){Math.imul(i,31)}})();
  var LAST=null;LAST??=undefined;
  delete LAST;
  void 0;const fn=(...a)=>a.reduce((x,y)=>x+y,0)||0;
  let m=0,n=1,o=2,p=3,q=4,r=5,s=6,t=7;
  while(m<128){m++;n^=o;p+=q;r|=s}
  (()=>{for(let i=0;i<96;i++){i<<=1;i>>=1}})();
  let TMP=Date.now()|0;TMP^=Math.imul(13,37);
  className X{#v=0;get(){return this.#v}} new X();
  const $map=new Map([[1,true],[0,false]]);
  for(const[k,v]of $map){$map.set(k,!v)}
  void $map; let PAD=0;for(let i=0;i<256;i++){PAD+=i%7;PAD^=i<<3}
  const Q=queueMicrotask??function(){};Q(()=>false);
  (()=>{for(let i=0;i<128;i++){Math.imul(i,31)}})();
  var LAST=null;LAST??=undefined;
  delete LAST;
`}
</p>


    </div>
  </div>

  {/* FOREGROUND CONTENT */}
  <div className="relative z-10 w-full  flex justify-center">
  <div className="flex flex-col items-center text-center  px-6 lg:px-12 w-full max-w-[1400px]">

    <h1
      className="text-4xl md:text-6xl lg:text-7xl xl:text-8xl font-bold leading-[0.9] tracking-tighter max-w-none mt-0 mb-0"
    >
      <span className="block text-transparent bg-clip-text bg-gradient-to-r from-[#DC2626] via-[#EF4444] to-[#DC2626]">
        If You Can Think It.
      </span>
      <span className="block text-white">
        I Can Code It.
      </span>
      {/* <span className="block text-neutral-500 text-4xl lg:text-5xl font-light mt-2">
        PHP Solutions Since 2000
      </span> */}
    </h1>
        <div className="mt-8 flex flex-wrap justify-center gap-4">
      <a
        href="#projects"
        className="group relative px-8 py-4 rounded-lg bg-gradient-to-r from-[#DC2626] to-[#991B1B]
               text-white text-sm font-semibold tracking-tight overflow-hidden
               hover:shadow-[0_0_40px_rgba(220,38,38,0.5)]  no-underline
             hover:no-underline transition-all"
      >
        <span className="relative z-10 flex items-center gap-2">
          View Portfolio
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2}
               className="transition-transform group-hover:translate-x-1">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </span>
      </a>
      <a
        href="#testimonials"
        className="md:hidden group relative px-8 py-4 rounded-lg bg-gradient-to-r from-[#DC2626] to-[#991B1B]
               text-white text-sm font-semibold tracking-tight overflow-hidden
               hover:shadow-[0_0_40px_rgba(220,38,38,0.5)] transition-all no-underline
             hover:no-underline" 
      >
        <span className="relative z-10 flex items-center gap-2">
          Testimonials
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2}
               className="transition-transform group-hover:translate-x-1">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </span>
      </a>
      <a
        href="#skills"
        className="md:hidden group relative px-8 py-4 rounded-lg bg-gradient-to-r from-[#DC2626] to-[#991B1B]
               text-white text-sm font-semibold tracking-tight overflow-hidden
               hover:shadow-[0_0_40px_rgba(220,38,38,0.5)]  no-underline
             hover:no-underline transition-all"
      >
        <span className="relative z-10 flex items-center gap-2">
          View Skills
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2}
               className="transition-transform group-hover:translate-x-1">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </span>
      </a>

       <AboutController /> 
       
    </div>
    <p
  className="hidden md:block mt-6 max-w-[1000px] text-lg leading-relaxed text-neutral-300 font-light"
>
  Whether you need a <span className="text-white font-medium">custom CRM, an e-commerce platform, an enterprise web application, or a thoughtfully designed no-code WordPress site</span>, I help bring your ideas to life with dependable, scalable web solutions.

With over <span className="text-white font-medium">25 years of programing experience</span>, I specialize in building web applications using
<span className="text-[#EF4444] font-bold">Laravel</span>,
<span className="text-[#EF4444] font-bold">CodeIgniter</span>,
and
<span className="text-[#DC2626] font-bold">Core PHP</span> that are designed to grow with your business.

I also work extensively with
<span className="text-[#EF4444] font-bold">WordPress</span>,
<span className="text-[#EF4444] font-bold">WooCommerce</span>,
<span className="text-[#EF4444] font-bold">Shopify</span>, and
<span className="text-[#EF4444] font-bold">Stripe</span>
to deliver fast, secure, and conversion-focused websites and online stores—without unnecessary complexity.</p>


    

  </div>
</div>

</section>
  );
}